# Frontend variables are injected at Vercel build time.
NEXT_PUBLIC_API_URL=https://backend-domain.com
NEXT_PUBLIC_API_PREFIX=/api/v1
NEXT_PUBLIC_DEPLOYMENT_MODE=public-demo

# FastAPI container variables.
APP_ENV=production
APP_MODE=cloud
API_HOST=0.0.0.0
API_PORT=8000
LOG_LEVEL=INFO
DOCS_ENABLED=false
CORS_ORIGINS=https://www.example.com,https://example.com
# Railway health checks use healthcheck.railway.app as the Host header.
ALLOWED_HOSTS=api.example.com,healthcheck.railway.app
MAX_XRK_UPLOAD_BYTES=52428800
XRK_PARSE_TIMEOUT_SECONDS=60
XRK_MAX_CONCURRENT_IMPORTS=2
XRK_RATE_LIMIT_PER_HOUR=10
XRK_MAX_RESPONSE_ROWS=30000

# The current MVP adapter uses SQLite. Replace it with the documented
# PostgreSQL repository before enabling multi-user persistence.
DATABASE_URL=sqlite:////app/storage/sessions.sqlite3
STORAGE_BACKEND=local
TASK_QUEUE_BACKEND=inline

# Target production values after the upload/worker phase:
# DATABASE_URL=postgresql+psycopg://user:password@host/database
# STORAGE_BACKEND=s3
# OBJECT_STORAGE_BUCKET=racing-telemetry-production
# OBJECT_STORAGE_ENDPOINT=https://s3.example.com
# OBJECT_STORAGE_REGION=auto
# TASK_QUEUE_BACKEND=redis
# REDIS_URL=rediss://user:password@host:6379/0
